// ***************** String Method *****************

// Immutable = in the javascript all strings are immutable .
// meaning of immutable 
// = 1). No changes can be made to strings.
// 2). Whenever we do try to make a change, a new string is created and old one remains same.

let myMsg = "Shubham        ";
console.log(myMsg);
let myNewMsg = myMsg.trim();
console.log(myNewMsg);