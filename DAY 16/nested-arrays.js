// *************** Nested Arrays(Multi Dimentional Arrays) ***************
// Nested Arrays = A nested array is an array that contains one or more arrays inside it. In simple words, it is an array within another array.
// syntax = arrayName = [[arr1], [arra2], ..... [arrN]];
// Example = 

// 1).
const nestedArr = [[1, 2, 3], [2, 4, 6], [3, 6, 9, 12]];
console.log(nestedArr);

// Note - We always visualize nested array in the form of table or Rows & Columns  


// Nested arrays can have multiple levels